# Version control
.git
.gitignore

# IDE / editor
.vscode
.idea
*.swp
*.swo

# Documentation (keep README for reference but exclude others)
PLAN.md
LICENSE

# CI / config (not needed in build context)
.github

# Node / frontend build artifacts
ui/node_modules
ui/dist
ui/.vite

# Rust build artifacts
server/target

# Data / runtime
server/data
/data
/thumbs
*.db
*.db-shm
*.db-wal
*.db-journal
.env
